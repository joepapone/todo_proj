Todo Project

 — Django (SQLite) 
— Minimal, working example

Here’s a small, ready-to-run Django Todo application using the default SQLite database. It includes models, views, URLs, templates and instructions so you can get it running locally quickly.
